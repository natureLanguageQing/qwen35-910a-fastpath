# Notice

This package contains configuration notes, benchmark summaries, and lightweight test utilities only.

It does not include:

- model weights
- proprietary SDK binaries
- private service endpoints
- credentials
- internal hostnames
- private benchmark logs that include infrastructure details

Before publishing, re-run your own benchmarks and replace any environment-specific paths with public placeholders.

